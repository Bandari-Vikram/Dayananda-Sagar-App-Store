"use client";

import { MenuOrbitIcon } from "@/components/home/menu-orbit-icon";

function AppsGridIcon({ className }: { className?: string; size?: number }) {
  return <MenuOrbitIcon className={className} />;
}

export { AppsGridIcon };
